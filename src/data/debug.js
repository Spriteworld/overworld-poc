let debug = {
  objects: !true,
  grid: !true,
  stateMachine: {
    console: false,
  },
  functions: {
    gameMap: !true,
    timeOverlay: !false,
    characterSeen: !true,
    characterRect: !true,
  },
};

export default debug;
